// Code generated by Windows API Generator.
// Do not modify manually.

package windows.win32.system.com;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodHandles;
import java.lang.invoke.VarHandle;

import static java.lang.foreign.ValueLayout.*;

/**
 * {@code IUnknown} COM interface
 *
 * @see <a href="https://learn.microsoft.com/windows/win32/api/unknwn/nn-unknwn-iunknown">IUnknown (Microsoft)</a>
 */
public interface IUnknown {
    /**
     * {@code QueryInterface} COM interface method
     * <p>
     * {@snippet lang=c :
     * HRESULT QueryInterface(
     *     GUID* riid,
     *     void** ppvObject
     * );
     * }
     * </p>
     */
    int QueryInterface(MemorySegment riid, MemorySegment ppvObject);

    /**
     * {@code AddRef} COM interface method
     * <p>
     * {@snippet lang=c :
     * DWORD AddRef(
     * );
     * }
     * </p>
     */
    int AddRef();

    /**
     * {@code Release} COM interface method
     * <p>
     * {@snippet lang=c :
     * DWORD Release(
     * );
     * }
     * </p>
     */
    int Release();

    /**
     * Wraps the given COM object in a Java object with methods to call the COM interface functions.
     */
    static IUnknown wrap(MemorySegment comObject) {
        return new $DOWNCALL(comObject.reinterpret(8L));
    }

    /**
     * Gets the address layout for pointers to {@code IUnknown} COM interfaces.
     */
    static AddressLayout addressLayout() {
        return IUnknown$ADDRESS_LAYOUT;
    }

    /**
     * Interface identifier (IID) for {@code IUnknown} ({@code {00000000-0000-0000-c000-000000000046}}).
     */
    static MemorySegment iid() {
        return $IID$IUnknown.IID$SEG;
    }

    /**
     * Creates a COM object instance for the given Java object implementing {@code IUnknown}.
     */
    static MemorySegment create(IUnknown obj, Arena arena) {
        var unwrapper = new $UPCALL(obj);
        var vtable = arena.allocate(ADDRESS, 3);
        var linker = Linker.nativeLinker();
        for (int i = 0; i < 3; i++)
            vtable.set(ADDRESS, 8 * i, linker.upcallStub($UPCALL_IMPL.HANDLES[i].bindTo(unwrapper), $UPCALL_IMPL.DESCRIPTORS[i], arena));
        var objSegment = arena.allocate(ADDRESS);
        objSegment.set(ADDRESS, 0, vtable);
        return objSegment;
    }

    StructLayout IUnknown$COM_OBJECT_LAYOUT = MemoryLayout.structLayout(
        ADDRESS.withTargetLayout(MemoryLayout.sequenceLayout(3, ADDRESS)).withName("vtable")
    );

    AddressLayout IUnknown$ADDRESS_LAYOUT = ADDRESS.withTargetLayout(IUnknown$COM_OBJECT_LAYOUT);

    AddressLayout ADDRESS$ADDRESS = ADDRESS.withTargetLayout(ADDRESS);
    AddressLayout ADDRESS$STRUCT_16_4 = ADDRESS.withTargetLayout(MemoryLayout.sequenceLayout(16, JAVA_BYTE).withByteAlignment(4));

    class $IID$IUnknown {
        private static final Arena ARENA = Arena.ofAuto();

        private static MemorySegment createGuid(long v1, long v2) {
            var seg = ARENA.allocate(16, 8);
            seg.set(ValueLayout.JAVA_LONG, 0, v1);
            seg.set(ValueLayout.JAVA_LONG, 8, v2);
            return seg;
        }

        private static final MemorySegment IID$SEG = createGuid(0L, 5044031582654955712L);

    }

    class $DOWNCALL implements IUnknown {
        private static final VarHandle VTABLE_FUNC_VARHANDLE = IUnknown$COM_OBJECT_LAYOUT.varHandle(
                PathElement.groupElement("vtable"),
                PathElement.dereferenceElement(),
                PathElement.sequenceElement()
        );

        private static MemorySegment vtableFunc(MemorySegment comObject, long index) {
            return (MemorySegment) VTABLE_FUNC_VARHANDLE.get(comObject, 0L, index);
        }

        public final MemorySegment comObject;

        protected $DOWNCALL(MemorySegment comObject) {
            this.comObject = comObject;
        }

        public int QueryInterface(MemorySegment riid, MemorySegment ppvObject) {
            try {
                return (int) VFUNC0$IMPL.HANDLE.invokeExact(vtableFunc(comObject, 0), comObject, riid, ppvObject);
            } catch (Throwable ex) {
                throw new RuntimeException(ex);
            }
        }

        public int AddRef() {
            try {
                return (int) VFUNC1$IMPL.HANDLE.invokeExact(vtableFunc(comObject, 1), comObject);
            } catch (Throwable ex) {
                throw new RuntimeException(ex);
            }
        }

        public int Release() {
            try {
                return (int) VFUNC2$IMPL.HANDLE.invokeExact(vtableFunc(comObject, 2), comObject);
            } catch (Throwable ex) {
                throw new RuntimeException(ex);
            }
        }

    }

    class VFUNC0$IMPL {
        private static final FunctionDescriptor DESC = FunctionDescriptor.of(JAVA_INT, IUnknown$ADDRESS_LAYOUT, ADDRESS$STRUCT_16_4, ADDRESS$ADDRESS);
        private static final MethodHandle HANDLE = Linker.nativeLinker().downcallHandle(DESC);
    }

    class VFUNC1$IMPL {
        private static final FunctionDescriptor DESC = FunctionDescriptor.of(JAVA_INT, IUnknown$ADDRESS_LAYOUT);
        private static final MethodHandle HANDLE = Linker.nativeLinker().downcallHandle(DESC);
    }

    class VFUNC2$IMPL {
        private static final FunctionDescriptor DESC = FunctionDescriptor.of(JAVA_INT, IUnknown$ADDRESS_LAYOUT);
        private static final MethodHandle HANDLE = Linker.nativeLinker().downcallHandle(DESC);
    }

    class $UPCALL {
        private final IUnknown javaObject;

        private $UPCALL(IUnknown javaObject) {
            this.javaObject = javaObject;
        }

        int QueryInterface(MemorySegment thisObject, MemorySegment riid, MemorySegment ppvObject) {
            return javaObject.QueryInterface(riid, ppvObject);
        }
        int AddRef(MemorySegment thisObject) {
            return javaObject.AddRef();
        }
        int Release(MemorySegment thisObject) {
            return javaObject.Release();
        }
    }

    class $UPCALL_IMPL {
        private static final FunctionDescriptor[] DESCRIPTORS = createDescriptors();
        private static final MethodHandle[] HANDLES = createHandles();

        private static FunctionDescriptor[] createDescriptors() {
            var descriptors = new FunctionDescriptor[3];
            descriptors[0] = FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS$STRUCT_16_4, ADDRESS$ADDRESS);
            descriptors[1] = FunctionDescriptor.of(JAVA_INT, ADDRESS);
            descriptors[2] = FunctionDescriptor.of(JAVA_INT, ADDRESS);
            return descriptors;
        }

        private static MethodHandle[] createHandles() {
            try {
                var lookup = MethodHandles.lookup();
                var handles = new MethodHandle[3];
                handles[0] = lookup.findVirtual($UPCALL.class, "QueryInterface", DESCRIPTORS[0].toMethodType());
                handles[1] = lookup.findVirtual($UPCALL.class, "AddRef", DESCRIPTORS[1].toMethodType());
                handles[2] = lookup.findVirtual($UPCALL.class, "Release", DESCRIPTORS[2].toMethodType());
               return handles;
           } catch (ReflectiveOperationException e) {
               throw new RuntimeException(e);
           }
       }
   }
}
