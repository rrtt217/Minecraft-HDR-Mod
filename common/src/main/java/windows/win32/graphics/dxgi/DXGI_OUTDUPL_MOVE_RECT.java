// Code generated by Windows API Generator.
// Do not modify manually.

package windows.win32.graphics.dxgi;

import java.lang.foreign.GroupLayout;
import java.lang.foreign.MemoryLayout;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.SegmentAllocator;

/**
 * {@code DXGI_OUTDUPL_MOVE_RECT} structure
 * <p>
 * {@snippet lang=c :
 * struct DXGI_OUTDUPL_MOVE_RECT {
 *    POINT SourcePoint;
 *    RECT DestinationRect;
 * }
 * }
 * </p>
 *
 * @see <a href="https://learn.microsoft.com/windows/win32/api/dxgi1_2/ns-dxgi1_2-dxgi_outdupl_move_rect">DXGI_OUTDUPL_MOVE_RECT (Microsoft)</a>
 */
public class DXGI_OUTDUPL_MOVE_RECT {

    private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
        windows.win32.foundation.POINT.layout().withName("SourcePoint"),
        windows.win32.foundation.RECT.layout().withName("DestinationRect")
    );

    /**
     * Gets the layout of the struct {@code DXGI_OUTDUPL_MOVE_RECT}.
     */
    public static GroupLayout layout() {
        return $LAYOUT;
    }

    /**
     * Gets the size of the struct {@code DXGI_OUTDUPL_MOVE_RECT} (in bytes).
     */
    public static long sizeof() {
        return 24L;
    }

    /**
     * Gets the offset of the field {@code SourcePoint} (in bytes).
     */
    public static long SourcePoint$offset() {
        return 0L;
    }

    /**
     * Gets the field {@code SourcePoint} of {@code segment}.
     * <p>
     * The returned memory segment is a slice of {@code segment} and shares the backing memory.
     * </p>
     */
    public static MemorySegment SourcePoint(MemorySegment segment) {
        return segment.asSlice(0L, 8L, 4L);
    }

    /**
     * Copies {@code value} into the field {@code SourcePoint} in {@code segment}.
     */
    public static void SourcePoint(MemorySegment segment, MemorySegment value) {
        MemorySegment.copy(value, 0L, segment, 0L, 8L);
    }

    /**
     * Gets the offset of the field {@code DestinationRect} (in bytes).
     */
    public static long DestinationRect$offset() {
        return 8L;
    }

    /**
     * Gets the field {@code DestinationRect} of {@code segment}.
     * <p>
     * The returned memory segment is a slice of {@code segment} and shares the backing memory.
     * </p>
     */
    public static MemorySegment DestinationRect(MemorySegment segment) {
        return segment.asSlice(8L, 16L, 4L);
    }

    /**
     * Copies {@code value} into the field {@code DestinationRect} in {@code segment}.
     */
    public static void DestinationRect(MemorySegment segment, MemorySegment value) {
        MemorySegment.copy(value, 0L, segment, 8L, 16L);
    }

    /**
     * Allocates a new memory segment for the struct {@code DXGI_OUTDUPL_MOVE_RECT}.
     */
    public static MemorySegment allocate(SegmentAllocator allocator) {
        return allocator.allocate(24L, 4L);
    }

    /**
     * Gets the element with index {@code index} of the specified {@code DXGI_OUTDUPL_MOVE_RECT} array.
     * <p>
     * The returned memory segment is a slice of {@code array} and shares the backing memory.
     * </p>
     */
    public static MemorySegment elementAsSlice(MemorySegment array, long index) {
        return array.asSlice(24L * index, 24L);
    }

    /**
     * Allocates an array of {@code elementCount} elements of this struct.
     */
    public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
        return allocator.allocate(24L * elementCount, 4L);
    }

    private DXGI_OUTDUPL_MOVE_RECT () {}
}
