// Code generated by Windows API Generator.
// Do not modify manually.

package windows.win32.devices.display;

import java.lang.foreign.GroupLayout;
import java.lang.foreign.MemoryLayout;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.SegmentAllocator;

/**
 * {@code DISPLAYCONFIG_TARGET_MODE} structure
 * <p>
 * {@snippet lang=c :
 * struct DISPLAYCONFIG_TARGET_MODE {
 *    DISPLAYCONFIG_VIDEO_SIGNAL_INFO targetVideoSignalInfo;
 * }
 * }
 * </p>
 *
 * @see <a href="https://learn.microsoft.com/windows/win32/api/wingdi/ns-wingdi-displayconfig_target_mode">DISPLAYCONFIG_TARGET_MODE (Microsoft)</a>
 */
public class DISPLAYCONFIG_TARGET_MODE {

    private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
        DISPLAYCONFIG_VIDEO_SIGNAL_INFO.layout().withName("targetVideoSignalInfo")
    );

    /**
     * Gets the layout of the struct {@code DISPLAYCONFIG_TARGET_MODE}.
     */
    public static GroupLayout layout() {
        return $LAYOUT;
    }

    /**
     * Gets the size of the struct {@code DISPLAYCONFIG_TARGET_MODE} (in bytes).
     */
    public static long sizeof() {
        return 48L;
    }

    /**
     * Gets the offset of the field {@code targetVideoSignalInfo} (in bytes).
     */
    public static long targetVideoSignalInfo$offset() {
        return 0L;
    }

    /**
     * Gets the field {@code targetVideoSignalInfo} of {@code segment}.
     * <p>
     * The returned memory segment is a slice of {@code segment} and shares the backing memory.
     * </p>
     */
    public static MemorySegment targetVideoSignalInfo(MemorySegment segment) {
        return segment.asSlice(0L, 48L, 8L);
    }

    /**
     * Copies {@code value} into the field {@code targetVideoSignalInfo} in {@code segment}.
     */
    public static void targetVideoSignalInfo(MemorySegment segment, MemorySegment value) {
        MemorySegment.copy(value, 0L, segment, 0L, 48L);
    }

    /**
     * Allocates a new memory segment for the struct {@code DISPLAYCONFIG_TARGET_MODE}.
     */
    public static MemorySegment allocate(SegmentAllocator allocator) {
        return allocator.allocate(48L, 8L);
    }

    /**
     * Gets the element with index {@code index} of the specified {@code DISPLAYCONFIG_TARGET_MODE} array.
     * <p>
     * The returned memory segment is a slice of {@code array} and shares the backing memory.
     * </p>
     */
    public static MemorySegment elementAsSlice(MemorySegment array, long index) {
        return array.asSlice(48L * index, 48L);
    }

    /**
     * Allocates an array of {@code elementCount} elements of this struct.
     */
    public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
        return allocator.allocate(48L * elementCount, 8L);
    }

    private DISPLAYCONFIG_TARGET_MODE () {}
}
