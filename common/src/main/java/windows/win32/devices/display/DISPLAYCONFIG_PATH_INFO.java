// Code generated by Windows API Generator.
// Do not modify manually.

package windows.win32.devices.display;

import java.lang.foreign.GroupLayout;
import java.lang.foreign.MemoryLayout;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.SegmentAllocator;

import static java.lang.foreign.ValueLayout.JAVA_INT;

/**
 * {@code DISPLAYCONFIG_PATH_INFO} structure
 * <p>
 * {@snippet lang=c :
 * struct DISPLAYCONFIG_PATH_INFO {
 *    DISPLAYCONFIG_PATH_SOURCE_INFO sourceInfo;
 *    DISPLAYCONFIG_PATH_TARGET_INFO targetInfo;
 *    DWORD flags;
 * }
 * }
 * </p>
 *
 * @see <a href="https://learn.microsoft.com/windows/win32/api/wingdi/ns-wingdi-displayconfig_path_info">DISPLAYCONFIG_PATH_INFO (Microsoft)</a>
 */
public class DISPLAYCONFIG_PATH_INFO {

    private static final GroupLayout $LAYOUT = MemoryLayout.structLayout(
        DISPLAYCONFIG_PATH_SOURCE_INFO.layout().withName("sourceInfo"),
        DISPLAYCONFIG_PATH_TARGET_INFO.layout().withName("targetInfo"),
        JAVA_INT.withName("flags")
    );

    /**
     * Gets the layout of the struct {@code DISPLAYCONFIG_PATH_INFO}.
     */
    public static GroupLayout layout() {
        return $LAYOUT;
    }

    /**
     * Gets the size of the struct {@code DISPLAYCONFIG_PATH_INFO} (in bytes).
     */
    public static long sizeof() {
        return 72L;
    }

    /**
     * Gets the offset of the field {@code sourceInfo} (in bytes).
     */
    public static long sourceInfo$offset() {
        return 0L;
    }

    /**
     * Gets the field {@code sourceInfo} of {@code segment}.
     * <p>
     * The returned memory segment is a slice of {@code segment} and shares the backing memory.
     * </p>
     */
    public static MemorySegment sourceInfo(MemorySegment segment) {
        return segment.asSlice(0L, 20L, 4L);
    }

    /**
     * Copies {@code value} into the field {@code sourceInfo} in {@code segment}.
     */
    public static void sourceInfo(MemorySegment segment, MemorySegment value) {
        MemorySegment.copy(value, 0L, segment, 0L, 20L);
    }

    /**
     * Gets the offset of the field {@code targetInfo} (in bytes).
     */
    public static long targetInfo$offset() {
        return 20L;
    }

    /**
     * Gets the field {@code targetInfo} of {@code segment}.
     * <p>
     * The returned memory segment is a slice of {@code segment} and shares the backing memory.
     * </p>
     */
    public static MemorySegment targetInfo(MemorySegment segment) {
        return segment.asSlice(20L, 48L, 4L);
    }

    /**
     * Copies {@code value} into the field {@code targetInfo} in {@code segment}.
     */
    public static void targetInfo(MemorySegment segment, MemorySegment value) {
        MemorySegment.copy(value, 0L, segment, 20L, 48L);
    }

    /**
     * Gets the offset of the field {@code flags} (in bytes).
     */
    public static long flags$offset() {
        return 68L;
    }

    /**
     * Gets the value of field {@code flags} in {@code segment}.
     */
    public static int flags(MemorySegment segment) {
        return segment.get(JAVA_INT, 68L);
    }

    /**
     * Sets the field {@code flags} in {@code segment} to {@code value}.
     */
    public static void flags(MemorySegment segment, int value) {
        segment.set(JAVA_INT, 68L, value);
    }

    /**
     * Allocates a new memory segment for the struct {@code DISPLAYCONFIG_PATH_INFO}.
     */
    public static MemorySegment allocate(SegmentAllocator allocator) {
        return allocator.allocate(72L, 4L);
    }

    /**
     * Gets the element with index {@code index} of the specified {@code DISPLAYCONFIG_PATH_INFO} array.
     * <p>
     * The returned memory segment is a slice of {@code array} and shares the backing memory.
     * </p>
     */
    public static MemorySegment elementAsSlice(MemorySegment array, long index) {
        return array.asSlice(72L * index, 72L);
    }

    /**
     * Allocates an array of {@code elementCount} elements of this struct.
     */
    public static MemorySegment allocateArray(long elementCount, SegmentAllocator allocator) {
        return allocator.allocate(72L * elementCount, 4L);
    }

    private DISPLAYCONFIG_PATH_INFO () {}
}
